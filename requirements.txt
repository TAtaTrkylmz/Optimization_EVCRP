# Pinned for reproducible installs across machines.
# To refresh pins after testing upgrades: pip install -U <pkg> && pip freeze | grep -E '^(pandas|requests|PuLP)='

numpy>=1.26
pandas==2.2.3
requests==2.32.3
streamlit>=1.45
qrcode[pil]>=8.0
matplotlib>=3.8
contextily>=1.6
